
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>ajax</title>
</head>
<body>
    <div> 
        <div id="div1">div1</div>
        @Ajax.ActionLink("点我", "ajaxActionShow", new AjaxOptions { HttpMethod="post",UpdateTargetId="div1",InsertionMode=InsertionMode.InsertAfter})
        @using (Ajax.BeginForm("addAjax", new AjaxOptions { HttpMethod = "post", UpdateTargetId = "div1", InsertionMode = InsertionMode.InsertAfter }))
        {
            <table>
                <tr><td>姓名：</td><td>@Html.TextBox("sName")</td></tr>

                <tr><td><input type="submit" value="提交" /></td></tr>
            </table>
        }
    </div>
</body>
</html>
